// array and object

//  everything in javascript known as "Object"

// using array
// array index starts from zero

let school = [ "Bachpan", "Primary", "Highschool"];
console.log(school)
console.log(typeof school)

console.log(Array.isArray(school))  //  ---- to know the array type we can use Array.isArray it give true or false value

// using object
// object consists key pair values

let details = {
    Name : "Soumya ",
    Qualification : "MCA"
}
console.log(details);
console.log(typeof details)

